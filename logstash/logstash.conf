input {
    tcp {
        port => 5000
        codec => json
    }
}

filter {
    # Add your filtering logic here if needed
}

output {
    elasticsearch {
        hosts => ["http://elasticsearch:9200"] # Elasticsearch server address
        index => "logs-%{+YYYY.MM.dd}" # Index pattern for storing logs
    }
}
